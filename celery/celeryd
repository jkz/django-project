# Provide defaults for celery configuration. Specify local configuration in
# local.celeryd

# Name of nodes to start, here we have a single node (space separate multiple)
CELERYD_NODES="$NAME_node"

# Where to chdir at start.
CELERYD_CHDIR="$ROOT"

# Python interpreter from environment.
ENV_PYTHON="$VENV/bin/python"

# How to call "manage.py celeryd_multi"
CELERYD_MULTI="manage celeryd_multi"

# How to call "manage.py celeryctl"
CELERYCTL="manage celeryctl"

# Extra arguments to celeryd
CELERYD_OPTS="--time-limit=300 --concurrency=8"

# Name of the celery config module.
CELERY_CONFIG_MODULE="celeryconfig"

# %n will be replaced with the nodename.
CELERYD_LOG_FILE="$LOCAL/celery/%n.log"
CELERYD_PID_FILE="$LOCAL/celery/%n.pid"

# Workers should run as an unprivileged user.
CELERYD_USER="celery"
CELERYD_GROUP="celery"

# Override these defaults with local settings if present
if [ -f "$CELERY/local.celeryd" ];
then
    source "$CELERY/local.celeryd"
fi
